#get a an access token
curl -X POST -vu myclientid:mysecret http://localhost:8080/login/token -H "Accept: application/json" -d "password=mypassword&username=myusername&grant_type=password&scope=read%20write&client_secret=mysecret&client_id=myclientid"

#access to an url

curl http://localhost:8080/test -H "Authorization: Bearer mytoken"